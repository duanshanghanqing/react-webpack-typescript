import * as React from 'react';
import Title from './Title';

const Header: React.FC = () => {
    return (
        <div style={{ height: 40, backgroundColor: '#cccccc' }}>
            <Title />
        </div>
    );
};

export default Header;
